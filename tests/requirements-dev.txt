# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Dependencies required to run the offline unit test suite under tests/unit/.
# These intentionally mirror the minimum set of runtime deps the services
# import at module load time, plus standard testing utilities. No live
# service (LLM, Milvus, memory DB, guardrails) is required.

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# Pydantic models are used pervasively and we validate their behavior.
pydantic>=2.13

# FastAPI TestClient is used for memory_retriever HTTP tests.
fastapi==0.136.1
httpx>=0.27

# HTTP client mocked in chain_server tests.
requests>=2.33

# YAML parsing used by config loaders under test.
PyYAML>=6.0

# OpenAI types are referenced when mocking chat completion responses.
openai>=1.40

# Needed by memory_retriever, catalog_retriever, and chain_server imports.
SQLAlchemy>=2.0
numpy>=1.26
Pillow>=12.2

# Needed by chain_server.graph / chain_server.chatter on import.
langgraph==1.1.10
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.0.12
langgraph-sdk==0.3.15
langchain-core==1.3.3

# Needed by catalog_retriever.retriever on import.
langchain-text-splitters>=1.1.2
pymilvus>=2.6
